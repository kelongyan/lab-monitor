# Python & Virtual Environment
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
build/
dist/
*.egg-info/

# Weights & Models
*.pt
*.onnx
*.engine
*.tflite

# Local Media & Datasets (Do not commit video files)
videos/*
!videos/.gitkeep

# Output Logs, Screenshots & Cache
outputs/*
!outputs/.gitkeep
*.log
*.jsonl

# IDE & OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Route Archives & Reference PDFs (large binaries)
*.7z
docs/*.pdf
docs/routes/*.pdf

# 现场点位表：含真实摄像头 IP（172.20.x.x）、图纸点号与物理位置对照。
# 本仓库在 GitHub 上是公开的，这类设施网络信息不入库；它是相机拓扑的唯一依据，
# 请自行留存（压缩包 常规路线.7z / 随机路线.7z 内也有一份）。
docs/*.xlsx

# Agent Workspace
.workbuddy/

# 客户平面图渲染底图：与 docs/*.xlsx 同级敏感（设施图纸，公开仓库不入库）
assets/floorplan/
videos_low/

# 切源时的原片配置备份（scripts/transcode_lowres.py --switch-sources 生成）。
# 派生产物：内容与未切源时的 config/sources.json 相同，需要时脚本会自己生成。
config/sources_high.json

# —— 保留本地不入库 ——
# docs/ 整体转为本地化：已入库的 .md / png 仍随 git 历史可见，新加文件将被忽略；
# 若需"从库里把某个文件出库"，必须 git rm --cached <path>（gitignore 不会反向 untrack）。
# 三条规则组合：
#   docs/*    匹配直接子文件（docs/foo.md）
#   docs/**   匹配子目录（docs/images）
#   docs/**/* 匹配子目录里的文件（docs/images/foo.png）
# docs/** 单写只匹配目录不匹配文件，必须配合其他两条
docs/*
docs/**
docs/**/*
!docs/.gitkeep
